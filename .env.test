# ACP Test Environment Configuration
# Copy this to .env.test and customize for your environment

# ============================================
# Server Configuration
# ============================================
# ⚠️ PORT WARNING: If port 8000 is occupied by Splunk, use 8010 instead!
# Check QUICKSTART_SERVER.md for details
ACP_BASE_URL=http://localhost:8010
ACP_ADMIN_KEY=your_admin_key_here

# ACP Path Prefix (for flexible routing)
# Set this if ACP is mounted under a different prefix than /acp
# Examples: /acp, /api/acp, /v1/acp
ACP_PREFIX=/acp

# ============================================
# Test Mode Configuration
# ============================================
# Test Mode: local | staging | prod
# Controls safety guard behavior
ACP_TEST_MODE=local

# Safety: Set to true ONLY when you want to test real bookings
# NEVER set this to true in production without explicit approval
ACP_ALLOW_REAL_BOOKING_TESTS=false

# ============================================
# Test Data
# ============================================
ACP_TEST_PROPERTY_ID=cloudbeds_001

# ============================================
# Performance Test Settings
# ============================================
# Run performance tests (optional, informational)
ACP_RUN_PERFORMANCE=false

# Performance test parameters
ACP_PERF_ITERATIONS=50
ACP_PERF_DURATION=10
ACP_PERF_CONCURRENCY=50

# ============================================
# Database Paths
# ============================================
# Path to backend directory (relative to project root)
ACP_BACKEND_DIR=backend

# ============================================
# Safety Notes
# ============================================
# 1. Local mode: Real bookings allowed with flag, default dry_run=true
# 2. Staging mode: Real bookings allowed with flag, default dry_run=true  
# 3. Prod mode: Real bookings BLOCKED by default (admin mutation tests also blocked)
#
# Admin mutation tests (pause/resume): Blocked in prod mode
# Real booking tests: Require ACP_ALLOW_REAL_BOOKING_TESTS=true
